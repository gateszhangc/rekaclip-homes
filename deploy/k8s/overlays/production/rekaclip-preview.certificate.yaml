apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: rekaclip-preview-tls
  namespace: rekaclip
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  secretName: rekaclip-preview-tls
  dnsNames:
    - 144.91.77.245.rekaclip.sslip.io
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt-staging
  usages:
    - digital signature
    - key encipherment
