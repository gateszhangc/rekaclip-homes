apiVersion: v1
kind: Service
metadata:
  name: easyclaw-backend
  labels:
    app.kubernetes.io/name: easyclaw-backend
    app.kubernetes.io/part-of: easyclaw
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: easyclaw-backend
  ports:
    - name: http
      port: 5001
      targetPort: http
