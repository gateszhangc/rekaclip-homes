apiVersion: apps/v1
kind: Deployment
metadata:
  name: rekaclip
  labels:
    app.kubernetes.io/name: rekaclip
    app.kubernetes.io/part-of: rekaclip
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: rekaclip
  template:
    metadata:
      labels:
        app.kubernetes.io/name: rekaclip
        app.kubernetes.io/part-of: rekaclip
    spec:
      imagePullSecrets:
        - name: dokploy-fleet-ghcr
      containers:
        - name: rekaclip
          image: registry.144.91.77.245.sslip.io/rekaclip-web:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - secretRef:
                name: rekaclip-web-env
          ports:
            - name: http
              containerPort: 3000
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 20
            periodSeconds: 20
          startupProbe:
            tcpSocket:
              port: http
            periodSeconds: 5
            failureThreshold: 30
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
