apiVersion: v1
kind: Service
metadata:
  name: easyclaw
  labels:
    app.kubernetes.io/name: easyclaw
    app.kubernetes.io/part-of: easyclaw
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: easyclaw
  ports:
    - name: http
      port: 80
      targetPort: http
