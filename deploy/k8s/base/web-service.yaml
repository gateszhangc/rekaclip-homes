apiVersion: v1
kind: Service
metadata:
  name: rekaclip
  labels:
    app.kubernetes.io/name: rekaclip
    app.kubernetes.io/part-of: rekaclip
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: rekaclip
  ports:
    - name: http
      port: 80
      targetPort: http
